cd example

WORKSPACE=/workspace/
C=$(docker container create --rm -w $WORKSPACE qualified/factor:0.98 factor -i=factor-codewars.image -run=kata.tests)
docker container cp ./. $C:$WORKSPACE
docker container start --attach $C